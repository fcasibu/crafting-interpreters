#ifndef _UTILS_H_
#define _UTILS_H_

#include <stdbool.h>

bool is_whitespace(const char ch);
bool is_digit(const char ch);
bool is_alpha(const char ch);
bool is_alphanum(const char ch);

#endif // _UTILS_H_
